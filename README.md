# This repo consists of 3 files 
> The purpose is to help learners practice their Git/GitHub skills
### Here are some suggestions on what view can watch out:
> I expect that the user has forked the repo
1. git clone <git user forked url>
2. cd hitproj1
3. ls -la
> By now you can check the contents of the files present in the directory.
4. git remote add upstream <git original source url>
5. git remote -v
> You can view the links we have with `origin` and `upstream`.
6. git checkout -b branch-name
7. Now keep going make choice.
